name: cyber_chat_ai
description: AI-powered safe messaging with real-time cyberbullying detection
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:        ^1.0.8
  supabase_flutter:       ^2.3.4
  http:                   ^1.2.0
  google_fonts:           ^6.1.0
  timeago:                ^3.6.1
  uuid:                   ^4.3.3
  image_picker:           ^1.0.7
  permission_handler:     ^11.3.0
  fl_chart:               ^0.68.0   # bar chart for risk distribution
  url_launcher:           ^6.3.0    # open mail app
  mailer:                 ^6.1.0    # ← ADD this

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true